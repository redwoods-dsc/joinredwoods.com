---
interface Props {
  href: string;
}
const { href } = Astro.props;
---

<a class="back-link" href={href}>&larr; <slot /></a>

<style>
  .back-link {
    display: inline-block;
    margin-block-end: var(--space-md);
    font-size: var(--font-size-sm);
  }
</style>
