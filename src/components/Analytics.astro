{
  /* Loaded last, at the end of the document: the header trees are the LCP
    element on every page, and a measurement script has no business competing
    with them for bandwidth. `async` keeps it off the parser either way, but
    being discovered after the preloads is what puts it behind them in the
    fetch queue. */
}
<script is:inline async src="https://www.googletagmanager.com/gtag/js?id=G-921FGLHSZM"></script>
<script is:inline>
  window.dataLayer = window.dataLayer || [];
  function gtag() {
    dataLayer.push(arguments);
  }
  gtag('js', new Date());

  gtag('config', 'G-921FGLHSZM');
</script>
