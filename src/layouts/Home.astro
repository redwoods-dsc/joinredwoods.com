<header class="header">
  <p class="redwoods">Redwoods</p>
  <h1>Welcome</h1>
  <p>Let's build a super cool website here</p>
</header>

<style>
/**
 * 1. Temporary style because the page is too short right now, we can remove this as soon as we've got some content in place. Subtracts the Page wrapper's top+bottom padding so the header fills the viewport without forcing a scrollbar.
 */
.header {
  min-height: calc(100dvh - (var(--space-lg) * 2)); /* [1] */
  padding: var(--space-lg);
}

.redwoods {
  color: var(--color-orange);
}
</style>
